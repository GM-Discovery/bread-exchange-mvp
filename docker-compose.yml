services:
  bread-exchange:
    build: .
    container_name: bread-exchange
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    expose:
      - "8787"
    # Optional: if you want to be explicit
    environment:
      - NODE_ENV=production
